# Psych — operator reference

Strengths, ESF, and related **profile PDFs** for operator self-modeling. Reference material only — not compiled into the INCREMENTS app.

| File | Role |
|------|------|
| `THE_FOUR_WEEK_ARC.md` | Block I field document (Jun 2026) — **canonical md + daily audio during stack/cardio** (v9.1, frozen through arc) |
| `THE_FOUR_WEEK_ARC.pdf` | Typeset export — regenerate after md edits |
| `the-four-week-arc-tts.txt` | Plain-text TTS source — use if re-rendering audio (matches v9.1 doctrine fixes) |
| `arc-pdf-build/` | `build_pdf.py`, `arc_style.css`, `regenerate_arc_pdf.sh` |

**Regenerate PDF** (from repo root or Psych folder):

```bash
./arc-pdf-build/regenerate_arc_pdf.sh
```

Requires `weasyprint` (`pip install weasyprint`). **Audio:** read the markdown, not the PDF.

**Refinement doctrine (workstation):** Refinement is not inherently avoidance. The question is *where the next unit of information comes from* — instrument vs receiver. See `FORM-iOS/docs/BRICE_OS/BRICE_OPERATOR_KERNEL.md` § Refinement vs routing. Arc v9 frozen; typo-class fixes only (identity mischaracterizations, scanning noise).

Do not treat these as product doctrine unless explicitly promoted via `Docs/`.
