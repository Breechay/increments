# INCREMENTS

Personal operating system (cognitive orchestration) — part of **BRICE-OS**.

## Canonical layout

```text
INCREMENTS/
  App/
    Increments.xcodeproj      ← open this in Xcode
    Increments/               ← Swift sources + assets
  Docs/
    INCREMENTS_Master.md      ← architecture truth
    …                         ← voice, design doctrine
  Psych/
    …                         ← operator psych/profile PDFs (reference only)
  Archive/
    Past-Docs/                ← historical specs and backups
```

**Do not** use legacy paths (`INCREMENTS New Top Folder`, duplicate project trees).

## Workspace

Open `FORM-iOS/BRICE-OS.code-workspace` (canonical BRICE-OS workspace).

## Strategy

Cross-product orientation: `FORM-iOS/docs/BRICE_OS/BRICE_OPERATOR_PRODUCT_BRIEF.md` (targeted sections only).
